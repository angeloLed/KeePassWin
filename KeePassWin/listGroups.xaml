<Page
    x:Class="KeePassWin.GroupList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KeePassWin"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    
    xmlns:data="using:KeePassWin"
    
    >

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid>
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">

                <GridView x:Name="gridElements" ItemsSource="{x:Bind db.Groups}">
                    <GridView.ItemTemplate>
                        <DataTemplate x:DataType="data:GroupKeys">
                            <TextBlock FontSize="12" Text="{x:Bind Name}"></TextBlock>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
                <Button x:Name="buttonNewGroup" Content="New Group" Click="buttonNewGroup_Click" />

            </StackPanel>
        </Grid>
    </Grid>
</Page>
